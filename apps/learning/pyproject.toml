[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "market-hunter-learning"
version = "0.1.0"
description = "Historical backfill, feature engineering, and ML pipelines for Market Hunter"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
